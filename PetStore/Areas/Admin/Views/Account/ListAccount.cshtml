@model List<Model.EF.User>
@{
    ViewBag.Title = "ListAccount";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
<div class="row">
    <div class="col-12">
        <!-- Post TABLE -->
        <div class="box">
            <div class="box-header"><h3>Danh sách tài khoản khách hàng</h3></div>
            <div class="box-body overflow-scroll">
                <table>
                    <thead>
                        <tr>
                            <th width="50px">STT</th>
                            <th>Họ Tên</th>
                            <th width="100px">Giới tính</th>
                            <th width="250px">Email</th>
                            <th>Địa chỉ</th>
                            <th>Trạng thái</th>
                            <th>Tác vụ</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{ int count = 1;}
                        @foreach (var item in Model)
                        {
                            if (item.Role == 4)
                            {
                                <tr>
                                    <td>@count</td>
                                    @{count++;}

                                    <td width="200px">
                                        @item.FullName
                                    </td>
                                    @if (item.Gender == null)
                                    {
                                        <td>Chưa cập nhật</td>
                                    }
                                    @if (item.Gender == true)
                                    {
                                        <td>Nam</td>
                                    }
                                    @if (item.Gender == false)
                                    {
                                        <td>Nữ</td>
                                    }
                                    <td>@item.Email</td>
                                    @if (item.Address == null)
                                    {
                                        <td width="250px" style="padding-right:50px">Chưa cập nhật</td>
                                    }
                                    else
                                    {
                                        <td width="250px" style="padding-right:50px">@item.Address</td>
                                    }

                                    @if (item.Status == true)
                                    {


                                        <td>
                                            <div class="payment-status payment-paid">
                                                <div class="dot"></div>
                                                <span>Active</span>
                                            </div>
                                        </td>
                                    }
                                    else
                                    {
                                        <td>
                                            <div class="payment-status payment-pending">
                                                <div class="dot"></div>
                                                <span>Inactive</span>
                                            </div>
                                        </td>
                                    }

                                </tr>
                            }

                        }
                    </tbody>
                </table>
            </div>
        </div>
        <!-- END TABLE -->
    </div>
</div>
@* ========================================================================================= *@
<h1>Add a button here</h1>
<div class="row">
    <div class="col-12">
        <!-- TABLE -->
        <div class="box">
            <div class="box-header"><h3>Danh sách tài khoản nhân viên</h3></div>
            <div class="box-body overflow-scroll">
                <table>
                    <thead>
                        <tr>
                            <th width="50px">STT</th>
                            <th width="180px">Họ Tên</th>
                            <th width="100px">Chức vụ</th>
                            <th width="80px">Giới tính</th>
                            <th width="220px">Email</th>
                            <th width="100px">Ngày sinh</th>
                            <th>Địa chỉ</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{int count2 = 1;}
                        @foreach (var item in Model)
                        {
                            if (item.Role != 4)
                            {
                                <tr>
                                    <td>@count2</td>
                                    @{count2++;}
                                    <td>@item.FullName</td>

                                    <td>@item.Role1.Name</td>

                                    @if (item.Gender == null)
                                    {
                                        <td>Chưa cập nhật</td>
                                    }
                                    @if (item.Gender == true)
                                    {
                                        <td>Nam</td>
                                    }
                                    @if (item.Gender == false)
                                    {
                                        <td>Nữ</td>
                                    }
                                    <td>@item.Email</td>
                                    @if (item.Birthday == null)
                                    {
                                        <td>Chưa cập nhật</td>
                                    }
                                    else
                                    {
                                        <td>@item.Birthday.Value.ToString("dd-MM-yyyy")</td>
                                    }


                                    @if (item.Address == null)
                                    {
                                        <td>Chưa cập nhật</td>
                                    }
                                    else
                                    {
                                        <td>@item.Address</td>
                                    }
                                </tr>
                                }

                                }
                            </tbody>
                </table>
            </div>
        </div>
        <!-- END ORDERS TABLE -->
    </div>
</div>



